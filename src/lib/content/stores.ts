// Store listings. Paste each store URL here the day it goes live and the
// matching button on /platforms turns into a real link automatically.
// While a value is null the button shows "Coming soon" instead.
export const STORE_LINKS: Record<"microsoft" | "macos" | "ios" | "android", string | null> = {
  microsoft: null,
  macos: null,
  ios: null,
  android: null,
};
